import type { Config } from 'tailwindcss'

/** Tailwind v4: tokens live in src/app/globals.css @theme */
const config: Config = {
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
}

export default config
