import type { ReactNode } from 'react'

/** Route group for gallery & shared result views (URL unchanged) */
export default function AppSectionLayout({ children }: { children: ReactNode }) {
  return children
}
